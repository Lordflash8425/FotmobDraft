(()=>{
  const norm=s=>String(s||'').normalize('NFKD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
  const keepers=new Set(['konstantinos tzolakis','emiliano martinez','lukas hornicek','jordan pickford','caoimhin kelleher','djordje petrovic','james trafford','kjell scherpen','david raya','gianluigi donnarumma','marco bizot','robin roefs','senne lammens','zion suzuki','matz sels','bernd leno','carl rushworth','alisson becker','antonin kinsky','dean henderson','robert sanchez','bart verbruggen']);
  const injuryStatus={'jack hinshelwood':'out','mats wieffer':'out','mathias jensen':'out','georginio rutter':'back','nathan collins':'doubt','moises caicedo':'doubt','edward nketiah':'doubt','james garner':'out','hidemasa morita':'out','marcus rashford':'back','william osula':'out','amar dedic':'doubt','ibrahim sangare':'back','james maddison':'back','cristhian mosquera':'doubt','matty cash':'doubt','cody gakpo':'doubt','daniel james':'back'};
  const metaCache=new Map();let filter='all';
  function hasState(){try{return Array.isArray(state.players)}catch{return false}}
  function decorate(){if(!hasState()||!state.players.length)return;state.players.forEach(p=>{const n=norm(p.name);if(keepers.has(n))p.position='GK';if(injuryStatus[n])p.injuryStatus=injuryStatus[n]})}
  function ensureControls(){const box=document.querySelector('.searchbox');if(!box||document.getElementById('player-filter'))return;const wrap=document.createElement('div');wrap.id='player-filter';wrap.style.cssText='display:flex;gap:6px;flex-wrap:wrap;margin:-4px 0 12px';[['all','All'],['GK','Keepers'],['def','Defenders'],['mid','Midfielders'],['att','Attackers'],['injured','🚑 Injured']].forEach(([v,t])=>{const b=document.createElement('button');b.type='button';b.textContent=t;b.dataset.filter=v;b.className='btn';b.style.cssText='padding:6px 9px;font-size:11px';b.onclick=()=>{filter=v;document.querySelectorAll('#player-filter button').forEach(x=>x.classList.toggle('primary',x===b));renderPlayersEnhanced()};wrap.appendChild(b)});box.parentNode.insertBefore(wrap,box);wrap.firstChild.classList.add('primary')}
  function matchesFilter(p){if(filter==='all')return true;const pos=String(p.position||'').toLowerCase();if(filter==='GK')return pos==='gk'||pos.includes('keeper');if(filter==='injured')return p.injuryStatus==='out'||p.injuryStatus==='doubt';if(filter==='def')return /def|back|center|centre|full/.test(pos);if(filter==='mid')return /mid|wing|attacking/.test(pos)&&!/(striker|forward)/.test(pos);if(filter==='att')return /striker|forward|wing|attacking/.test(pos)&&pos!=='gk';return true}
  function photoMarkup(p){const fallback=(p.name||'?').split(/\s+/).map(x=>x[0]).slice(0,2).join('').toUpperCase();return p.photo?`<img class="avatar" src="${p.photo}" alt="" loading="lazy" onerror="this.style.display='none';this.nextElementSibling.style.display='grid'"/><span class="avatar-fallback">${fallback}</span>`:`<span class="avatar-fallback">${fallback}</span>`}
  function renderPlayersEnhanced(){if(!hasState()||typeof $!=='function')return;const q=$('search').value.trim().toLowerCase(),taken=allTaken(),arr=state.players.filter(p=>matchesFilter(p)&&`${p.name} ${p.teamName||''}`.toLowerCase().includes(q));$('count').textContent=`${arr.length} shown`;$('player-list').innerHTML=arr.slice(0,250).map(p=>{const injury=p.injuryStatus==='out'?'🩹':p.injuryStatus==='doubt'?'⚠️':'';return `<div class="player ${taken.has(key(p))?'used':''}" data-id="${key(p)}">${photoMarkup(p)}<div><div class="pname">${p.name} ${injury}</div><div class="meta">${p.teamName||'Premier League'} · ${p.position||'Player'}</div></div><div class="rating good">${Number(p.rating).toFixed(2)}</div></div>`}).join('')||'<div class="empty">No players match that search.</div>';$('player-list').querySelectorAll('.player').forEach(el=>el.onclick=()=>{const p=state.players.find(x=>key(x)===el.dataset.id);if(!p||taken.has(key(p)))return;state.selectedPlayer=p;document.querySelectorAll('.player').forEach(x=>x.style.outline='');el.style.outline='2px solid var(--accent)'});lazyPhotos(arr.slice(0,30))}
  async function getMeta(p){const n=norm(p.name);if(metaCache.has(n))return metaCache.get(n);const promise=fetch('/api/player-meta?name='+encodeURIComponent(p.name)).then(r=>r.ok?r.json():null).catch(()=>null);metaCache.set(n,promise);return promise}
  function lazyPhotos(players){players.forEach(p=>{if(p.photo||p._photoRequested)return;p._photoRequested=true;getMeta(p).then(m=>{if(m&&m.photo)p.photo=m.photo;if(m&&m.position&&!p.position)p.position=m.position;if(m&&(m.photo||m.position)){renderPlayersEnhanced();try{renderPitch()}catch{}}})})}
  const originalRenderAll=window.renderAll;window.renderPlayers=()=>{decorate();ensureControls();renderPlayersEnhanced()};window.renderAll=()=>{decorate();ensureControls();originalRenderAll();renderPlayersEnhanced()};
  const style=document.createElement('style');style.textContent='.avatar-fallback{width:36px;height:36px;border-radius:50%;background:var(--panel2);border:1px solid var(--line);display:grid;place-items:center;font-size:10px;font-weight:900;color:var(--muted)}.pname{display:flex;align-items:center;gap:4px}';document.head.appendChild(style);
  const boot=setInterval(()=>{if(hasState()&&state.players.length){clearInterval(boot);decorate();ensureControls();renderPlayersEnhanced()}},100);setTimeout(()=>clearInterval(boot),15000);
})();
